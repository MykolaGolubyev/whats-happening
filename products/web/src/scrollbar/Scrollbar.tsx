import * as React from 'react';
import './Scrollbar.css';

interface Props {
    date: string;
}

const Scrollbar = ({date}: Props) => {
    return (
        <div className="Container"></div>
    );
};

export {Scrollbar};