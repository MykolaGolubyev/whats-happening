import { EventInfo } from '../events/EventInfo';
import * as React from 'react';

import './LargeEventCard.css';

export const LargeEventCard = (event: EventInfo) => {
  return (
    <div>Large card</div>
  );
};